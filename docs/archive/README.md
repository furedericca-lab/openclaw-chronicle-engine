# Docs Archive

This folder preserves historical phase/task/status execution artifacts.

Archived scopes:

- `context-engine-split/`
- `remote-memory-backend/`

Canonical technical references remain in their active scope roots under `docs/context-engine-split/` and `docs/remote-memory-backend/`.
