# remote-memory-backend Docs Index

Snapshot refresh: `2026-03-18`

Current snapshot documents:

- `remote-memory-backend-contracts.md`
- `remote-memory-backend-2026-03-18-technical-documentation.md`

Snapshot clarification:

- this `2026-03-18` snapshot supersedes the `2026-03-17` remote-memory-backend snapshot where the two diverge;
- the main divergence is that **command-triggered reflection generation has been removed** from the supported runtime/backend contract surface;
- reflection is now **recall / injection only**;
- cadence-driven distill is now the only supported trajectory-derived knowledge-generation path;
- `session-lessons` owns lesson/cause/fix/prevention/stable-decision/durable-practice extraction;
- `governance-candidates` owns promotion-oriented governance outputs;
- `follow-up-focus` and `next-turn-guidance` are distill artifact subtypes replacing separate derived/open-loop reflection persistence;
- current canonical runtime guidance lives in `../runtime-architecture.md`, `../README.md`, and `../README_CN.md`.

Current-state rule:

- use this `2026-03-18` folder as the single current remote-memory-backend snapshot for the active backend/shell boundary after turns-stage distill unification.
- the older 2026-03-17 contract state is superseded by this refreshed snapshot and should not be treated as a parallel current reference.
